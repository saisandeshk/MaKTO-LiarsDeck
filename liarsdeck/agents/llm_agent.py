"""
ROADMAP ONLY

Planned responsibility:
- Convert observation to prompt.
- Query model backend.
- Parse model output to environment action.
- Emit structured Player_<id>.jsonl logs:
  {phase, prompt, response, message, gen_times}
"""
