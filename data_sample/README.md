# Data Sample (Scaffold)

ROADMAP ONLY.

Planned purpose:
- Define raw trajectory format for Liar's Deck.
- Define processing steps to generate SFT-friendly tables.

Expected raw files per game:
- `event_en.json`
- `note_en.json`

Note:
- During early synthetic-only phase, `note_en.json` can be generated by template-based or model-based annotators.
